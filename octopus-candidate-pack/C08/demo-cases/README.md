# Demo-only cases

Everything in this folder is SIMULATED, created for demo purposes to
exercise the C08 rules engine against scenarios beyond the one
official `initial.json` record. Per the exercise's own instruction
("Any additional simulated records you create must be labeled and
kept separate from the supplied test records"), these are kept in
their own folder and never merge with or alter the canonical
`../initial.json`.
